## What this changes

## Why

## Checklist

- [ ] `npm run conform` still ends with `5/5 match`, or this PR ships an ADR in `docs/adr/` explaining the verdict change.
- [ ] Tests cover the change (`uv run pytest` for Python; the conformance suite for the kernel and bridges).
- [ ] `uv run ruff format --check .`, `uv run ruff check .`, and `uv run mypy packages` pass.
- [ ] No file under `prior-art/a24-v1/` is edited.
- [ ] The word "autonomous" does not appear. Agents propose; humans sign.
- [ ] No capability claim without evidence in this repository.
- [ ] CHANGELOG.md has an entry under Unreleased.
- [ ] Every commit carries a DCO sign-off (`git commit -s`).

Signed-off-by: <your name> <your email>
